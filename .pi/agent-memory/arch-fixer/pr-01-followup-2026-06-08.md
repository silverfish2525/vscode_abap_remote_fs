---
name: PR-01 arch-fix2 follow-up (2026-06-08)
description: Re-implementation of PR-01 review concrete actions after first fixer no-op'd
type: project
---

# PR-01 cleanup-dead-deps — arch-fix2 follow-up

Branch: `chore/cleanup-dead-deps-and-native-swaps-arch-fix2` (sibling of `chore/cleanup-dead-deps-and-native-swaps`)
Commits stacked: 2

## What was done

1. **uuid v1→v4 doc-comments** added to:
   - `client/src/adt/debugger/debugListener.ts`
     - `getOrCreateIdeId` — persisted in `context.workspaceState["adt.ideId"]`
     - `getOrCreateTerminalId` — persisted in `~/.SAP/ABAPDebugging/terminalId` (POSIX) or Windows registry (HKCU\Software\SAP\ABAP Debugging\TerminalID, never written by us)
   - `client/src/views/abapgit.ts` — three `randomUUID()` tree-item IDs (ephemeral, no persistence concern; comment placed once at the first call site).
   - Comment template: "uuid v1 -> v4 transition (crypto.randomUUID): ... Format remains 32-hex-uppercase".

2. **`scripts/rmrf.mjs` glob expansion** — root `package.json` `clean` script uses `*.vsix`. POSIX shells expand pre-exec; Windows cmd.exe passes the literal pattern. New rmrf.mjs scans args for `*`/`?`/`[`, expands via `fs.readdirSync` + glob-to-regex (only `*` and `?` honoured, no brace/extglob/`**`). Smoke-tested: matches sibling files, tolerates missing dirs, non-glob args pass through.

## Lessons

- Previous fixer exited with "nothing to fix" because review prose was framed as "the code is fine, but you _could_ document...". User considers these CONCRETE actions when listed under recommendations. **Default: implement listed recommendations even when the verdict is positive, unless the review explicitly says "skip".**
- Verdict was likely "Worth merging with notes" → address explicit notes only.

## Verification gap

`npx tsc --noEmit` couldn't run — node_modules absent in this agent checkout (no `npm ci` step). Edits are comment-only in `.ts` files, so cannot affect typecheck. `rmrf.mjs` smoke-tested directly with `node`.
